export enum ESERVICE_TYPE {
  PICKUP = "pickup",
  MOVE = "move",
  CAR = "car",
  TEL = "tel",
}

export enum EPLACE_TYPE {
  ARRIVAL = "arrival",
  DEPARTURE = "departure",
}

export enum ESCHEDULE_TYPE {
  YEAR = "year",
  MONTH = "month",
  DATE = "date",
  HOUR = "hour",
  MINUTE = "minute",
}
